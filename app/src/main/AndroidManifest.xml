<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tritendence">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tritendence_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DarkRed"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".activities.TrainerInformationActivity"></activity>
        <activity android:name=".activities.AddAthleteActivity" />
        <activity android:name=".activities.AddGroupActivity" />
        <activity android:name=".activities.AthleteInformationActivity" />
        <activity android:name=".activities.GroupInformationActivity" />
        <activity android:name=".activities.AttendanceSheetActivity" />
        <activity android:name=".activities.RegistrationActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>